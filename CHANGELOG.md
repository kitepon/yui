# Changelog

## 2026-09-23

本番 image `yuihome:20260923-c41cb9b`。Bluetooth 押し `yuihome-switchbot-ble:20260923-c41cb9b`。

### 変わったこと

- Smart Life の名乗りが一時的に途切れても、直近30分以内にLANで読めた機器は最後の宛先で操作と読取を試す
- LAN読取に失敗した機器をIoT Coreへ切り替えず、失敗理由を表示する。カードでは失敗時に「LAN不可」、読取記録が古い場合はLAN印を外す
- Tuyaの機器一覧が返す `localKey` / `isOnline` / `productName` と、仕様の `dpId` を同期時に読む。機器一覧はAPIの20台上限に合わせてページを送る

### 限界

- 初回同期と新しい機器の鍵取得にはTuya IoT Platformが必要。LANに居ない機器とローカル版3.4 / 3.5はクラウド経路のまま

## 2026-09-22

本番 image `yuihome:20260922-66812c8`。Bluetooth 押し `yuihome-switchbot-ble:20260922-66812c8`。

### 変わったこと

- Smart Life の名乗り（UDP 6666 / 6667）を、ホストの LAN で直接受ける。機器への TCP 6668 もホストの受け役が中継する。容器から家の LAN へは届かない
- 水温計は LAN の実値で毎分更新する。Tuya クラウドの試用枠が切れていても、LAN にいる機器は読む

### 限界

- 機器が名乗りを出すまでの時間は縮まらない。3.1 は Smart Life アプリが LAN を握っているあいだ、数分黙ることがある
- Tuya IoT Core の試用枠が切れていると、LAN に居ない機器（3.4 / 3.5）はクラウドから読めない

## 2026-09-21

本番 image `yuihome:20260921-edfdb2a`。Bluetooth 押し `yuihome-switchbot-ble:20260921-edfdb2a`。

### 変わったこと

- Smart Life（Tuya）の機器を、同期のあとはクラウドを通さず LAN で読み書きする。同期で機器ごとの鍵と dp 対応を受け取って暗号化保存し、機器の LAN 上の居場所は機器自身の名乗り（UDP 6666 / 6667）を結が聞いて覚える。人が宛先や鍵を書く設定は無い
- 同じ LAN にいる 3.1 / 3.3 の機器はセンサー更新・スイッチの入／切の読み取り・操作を LAN で行い、クラウドへ問い合わせない。IoT Core の試用枠を毎分消費しない
- 機器カードに「LAN」の印、接続タブの Smart Life カードに機器ごとの経路・最終読取・失敗理由を出す。一度読めた印は、名乗りが途切れても最終読取から 30 分残る。画面側の保存で印は消えない
- `compose.yaml` が 6666/udp と 6667/udp を公開する。ホストの firewall で LAN からの UDP 6666 / 6667 を通す

### 限界

- Tuya IoT Platform の登録（Access ID / Secret / UID）は初回同期のために要る。鍵は Tuya のサーバーにしかなく、結が受け取る口はこれだけ
- LAN で読み書きできるのは Tuya ローカル version 3.1 と 3.3 の機器。3.4 / 3.5 と LAN に居ない機器はクラウドのまま
- 3.1 の古い機器は LAN 接続を 1 本しか受けない。Smart Life アプリを開いている間は結からの操作が失敗し、理由が接続タブに出る
- 名乗りが届かない環境（別 VLAN・firewall）では LAN は使われず、接続タブに「LAN に見つかりません」と出る

## v0.2.0 — 2026-09-19

v0.1.0 以降、2026-09-17 の本番 image `yuihome:20260917-9968945` までの公開 snapshot。中身は下の日付の記録のとおり。`v0.x` の間は database schema、設定、self-hosted upgrade の長期互換をまだ保証しない。

## 2026-09-17

本番 image `yuihome:20260917-9968945`。Bluetooth 押し `yuihome-switchbot-ble:20260917-9968945`。

### 変わったこと

- SwitchBot の Bluetooth 押しは、プロセスで一つのイベントループを使い、押すたびに D-Bus 接続を増やさない
- ボットが応答したあとの切断失敗は、押せなかったことにしない。空の例外は型名を出す
- オートメーションの「連続では動かさない」は機器ごとに付ける。古い全体設定は読み込み時に各機器へ移す

### 限界

- ボットがサーバーの近くにあること、ホストの bluetoothd が動いていることはこれまでどおり

## 2026-09-12

本番 image `yuihome:20260912-7116d41`。Bluetooth 押しは `yuihome-switchbot-ble:20260912-7116d41`。

### 変わったこと

- 「連続では動かさない」で再送を省く回も、条件成立中の上位ルールが同じ機器への優先権を保つ。上下のルールが交互に機器を切り替える不具合を修正

- オートメーションごとに「条件成立で下の判定を打ち切る」を設定できる。連続実行を省略する回も、条件成立中は下を判定・実行しない
- 配備時の Bluetooth 確認は、結アプリが使う接続先へ行う。ホストへ公開していないポートを確認して失敗する問題を修正

- 分析ページで、室温・湿度・外気温・水温の推移と、オートメーションが送った・動かさなかった判断を見られる
- 分析の項目は折り返して置く。横に流すと選べない
- 分析グラフは下のバーで1時間まで切れ、温度・湿度も同じバーで切る
- センサー範囲条件の上限は含めない。隣の範囲と同じ値で二重に当たらない

### 限界

- グラフは有効なオートメーションがある家だけ。tick が残してから出る
- 履歴は14日で消す。家のバックアップには載せない

## 2026-09-11

本番 image `yuihome:20260911-63ced03`。Bluetooth 押しは `yuihome-switchbot-ble:20260911-63ced03`。

### 変わったこと

- オートメーションに「連続では動かさない」を付けられる。直前に動いたオートメーションが自分自身なら何もしない
- SwitchBot の押すボットを、サーバーの Bluetooth で直接押せる
- SwitchBot の押すモードのボットは「押す」を送る。ハブに繋がっていないボットは「ハブなし」と出す
- ダイキンエアコンのカードは、停止中でも室温・湿度・外気温を出す

### 限界

- Bluetooth 直結は `YUI_SWITCHBOT_BLE_URL` と `COMPOSE_PROFILES=ble` がある結だけ。ボットが電波の届くところにあること。結からは同じ compose ネットワークの `http://switchbot-ble:18862` へ届ける
- クラウドから押すにはハブが要る。ハブ無しクラウドはオフライン
- 外気温は室外機が返すときだけ、エアコンの「値」に出る

## 2026-09-10

本番 image `yuihome:20260910-9c7d009`。

### 変わったこと

- ダイキン直結の外気温を、エアコンのセンサー項目としてオートメーションの条件に使える
- 独立の「外気温」カードはやめた。室温と同じくダイキンエアコンの値
- 条件を満たしたオートメーションが同じ機器を含むとき、一覧の上だけがその機器を動かす。下は残った機器だけ動く。並べ替えできる

### 限界

- 外気温は室外機が返すときだけ、エアコンの「値」に出る
- 「今すぐ」は並べ替えの優先を見ない。そのオートメーションの機器を全部動かす
- 条件を満たしているあいだは、一度送ったあとも上のオートメーションが同じ機器を取り続ける。下が奪わない。設定が違えば上の指示を出す

## 2026-08-28

本番 image `yuihome:20260828-f954a15`。

### 変わったこと

- ダイキン直結の風向に「自動」を足した（自動 100000 / 固定 000000 / スイング 0F0000）
- 自動運転で相対温度（p_1F）を読み書きする。外気温を表示する

### 限界

- 風向の固定羽根の多段位置は未解読
- パワフル・ストリーマなど、符号が実測で確定していない項目は出さない

## 2026-08-28

本番 image `yuihome:20260828-e15ff23`。

### 変わったこと

- オートメーションと場面で、画面に出ている初期値を触らなくても保存する。一覧と実行に、見た項目が載る
- センサー条件に「範囲内」を足した。範囲に入っているあいだ、いまの設定と違うときだけ送る
- 範囲条件は、設定を読み返せる機器だけに使える（ダイキン直結、Nature Remo のエアコン・照明、SwitchBot の実機、Smart Life）

### 限界

- すでに空のまま残っているアクションは、編集して保存し直すまで空のまま
- 時刻だけ欠けている記録は、読み込み時に 7:00 毎日とする
- 赤外線リモコン（SwitchBot IR・学習リモコン）とオーデリックには範囲条件を使えない
- 範囲の外では何もしない。以上・以下は、満たしているあいだ一度きり

## 2026-08-27

本番 image `yuihome:20260827-ca64b04`。

### 変わったこと

- オートメーションのセンサーしきい値（気温・湿度・照度）を、小数点第一位まで設定できる
- 室温がしきい値を下回っても、古い pass 記録のまま黙らない
- サーバーの確認周期で SwitchBot のセンサー値も取り直す

### 限界

- 時刻の時・分・間隔は整数のまま
- 条件を満たしているあいだは、一度動いたら、一度しきい値の外側へ出て戻るまで再発火しない

## 2026-08-26

本番 image `yuihome:20260825-b21e822`。

### 変わったこと

- Nature Remo・SwitchBot・Smart Life の機器種別と操作を、Home Assistant の機器表に合わせて広げた
- Nature Remo のエアコンで風量と風向（上下スイング／固定）を読み書きする
- SwitchBot は同期時に状態を取り、IR エアコンは温度・モード・風量をまとめて送る
- Smart Life は照明の明るさ、カーテンの開き、エアコンの温度などを、機器が持つ項目だけ送る
- Smart Life のセンサー（水温計を含む）は、空のときだけでなく毎分 status を取り直す。一覧の古い温度を残さない

### 限界

- Home Assistant 自体は同梱しない
- 掃除機・加湿器など、結に kind が無い機器は詳細操作を出さない
- Nature Remo の風向は左右の独立指定をしない
- Smart Life の表に無い category は other

## v0.1.0 — 2026-08-23

結 Yuiの最初の公開snapshotです。

### 届けるもの

- Nature Remo、SwitchBot、Smart Life（Tuya）を一つのホーム画面へ統合
- 対応するダイキンエアコンへLANから直接接続
- 複数機器を動かす場面と、時刻・機器・sensorを条件にしたserver-side automation
- Alexa Smart HomeとiPhoneのホーム画面追加
- Better Authによる認証、AES-256-GCMによる家電token暗号化、SQLite永続化
- MITのself-hosted版と、`yuihome.kitepon.dev`の公式hosted版

公式hosted版は月額100円または年額1,000円（税込）、初回30日間無料です。
self-hosted版は無料で、機能制限はありません。

### 現在の保証範囲

- ダイキン直結は2020年うるさらX 1機種で実測済み。同世代の他機種は未検証
- 2018年以前の後付けダイキンadapter（旧API）は未対応
- オーデリックのprotocolとprivate bridgeは配布対象外
- native iPhone appとproduction Cloudflare移転は後続工程
- `v0.x`の間はdatabase schema、設定、self-hosted upgradeの長期互換をまだ保証しない

不具合と対応機種の報告は[GitHub Issues](https://github.com/kitepon/yui/issues)、
脆弱性は[非公開の報告窓口](https://github.com/kitepon/yui/security/advisories/new)で受け付けます。
